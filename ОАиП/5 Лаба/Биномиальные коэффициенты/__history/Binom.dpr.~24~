program Binom;

{$APPTYPE CONSOLE}

procedure Factorial(var Num, FactNum: Int64);
var
  k: Byte;
begin
  k:= 1;
  if k < Num then
    begin
      inc(k);
      FactNum:= FactNum * k;
      Factorial(Num, FactNum, k);
    end;
end;

procedure Coefficient(Coeff,i: Integer);
var
  FactNum, Calc, Num: Int64;
  j: Integer;
begin
  FactNum:= 1;
  Calc:= 1;

  for j:= 0 to i - 1 do
    Calc:= Calc * (Coeff - j);

  Num:= i;
  Factorial(Num, FactNum);
  Calc:= Calc div FactNum;
  Write(Calc,' ');

  if i < Coeff then Coefficient(Coeff,i + 1);
end;

var
  n: Integer;
begin
  Readln(n);
  if n <> 0  then
    Coefficient(n, 0)
  else
    Write('1');
  Readln;
end.
